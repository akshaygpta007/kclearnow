import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  searchBox: {
    borderWidth: 1,
  },
  moviesList: {
    marginTop: 10,
  },
});

export default styles;
